config:
  send_anonymous_usage_stats: False
  use_colors: True

sdcs_data:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "localhost"
      user: "postgres"
      pass: "kermitbuns"
      port: 5432
      dbname: "sdcs"
      schema: "events" # Replace 'events' with the appropriate schema name for your database
